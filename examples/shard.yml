name: examples
version: 0.1.0

dependencies:
  kemal:
    github: kemalcr/kemal

targets:
  minimal:
    main: minimal.cr
  simple:
    main: simple.cr
  chat:
    main: chat.cr
  server:
    main: server.cr
  tokenize:
    main: tokenize.cr
